{
  "name": "notethread",
  "version": "1.0.0",
  "description": "NoteThread — bloco de notas em formato de chat/threads infinitas com sync em tempo real e offline-first.",
  "scripts": {
    "check": "node --check public/app.js && node --check public/sw.js && echo \"syntax ok\"",
    "test": "node --test tests/*.test.js",
    "e2e": "playwright test"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "sharp": "^0.33.0"
  }
}
